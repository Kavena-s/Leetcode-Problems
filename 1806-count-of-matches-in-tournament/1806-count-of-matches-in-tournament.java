class Solution {
    public int numberOfMatches(int n) {
        // int match = n/2;
        // if(n % 2 == 0) n /= 2;
        // else n = (n-1)/2 + 1;
        // int sum = match;
        // while(n > 1){
        //     match = n / 2;
        //     if(n % 2 == 0)n/= 2;
        //     else n = (n - 1)/2 + 1;
        //     sum += match;
        // }
        // return sum ;
        return n-1;
    }
}